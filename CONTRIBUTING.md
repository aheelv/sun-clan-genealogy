# 协作与提交信息规范

本文件约定本仓库的提交信息格式、分支策略与数据类变更的强制要求。

---

## 1. 提交信息规范

采用 **Conventional Commits**，描述语用中文（与项目文档一致）。

```
<类型>(<范围>): <主题>

<正文：做了什么、为什么这么做>

<脚注：Refs / Fixes>
```

### 1.1 类型（必填，取下列之一）

| 类型 | 用于 | 示例 |
| --- | --- | --- |
| `feat` | 新增功能或视图 | `feat(chart): 支持双击节点改根` |
| `fix` | 修复缺陷 | `fix(data): 纠正第 5 页世义父系误挂` |
| `data` | 重建数据（跑 `build:data` 后的结果变更） | `data: 前言补回原谱第 35 段开篇` |
| `docs` | 仅文档变更 | `docs(04): 补充转录口径说明` |
| `refactor` | 重构，不改变外部行为 | `refactor(layout): 抽出轮廓法为独立模块` |
| `test` | 自检脚本与断言 | `test(render): 锁定默认落点为概览` |
| `chore` | 工程杂项、依赖、配置 | `chore: 初始化仓库` |

### 1.2 范围（可选）

取模块或目录名，小写：`data` · `chart` · `explore` · `home` · `docs` · `validate` · `admin` ·
`store` · `schema` · `layout` · `render` · `tools`。

### 1.3 主题行

- 祈使句，描述"做了什么"，不写"修改了文件"。
- **不超过 50 个字符**，结尾不加句号。
- 不写 `update` / `修改代码` 这类无信息量的词。

### 1.4 正文

- 与主题行之间空一行；每行不超过 **72 个字符**。
- 重点回答**为什么**，而非重复 diff 已能看出的**是什么**。
- 涉及数据变更时，必须列出**变更前后的关键数字**。
- 正文可用 `-` 列点。

### 1.5 脚注

- `Refs: #12` 关联议题；`Fixes: #12` 关闭议题。

### 1.6 完整示例

```
fix(data): 纠正单一父块边界处的子女误挂

逐人「行距最近」会把连续子女块在其边界劈开，边缘子女被判给相邻的
无嗣父。第 5 页 J 列子女块 [271..282] 的世义(J271) 因此被送给块外的
怀裕(I269，行距 2)，而块内唯一父庆裕在 I276(行距 5)。

改为「行距最近 + 单一父块修正」：块内若只有一位父，则该块子女全归他。
修正只否决「最近父落在块外」，不整体替换逐人判定——全面替换会连带
改动 24 处无问题的挂接。

- 修正生效 12 处（V017）
- 父系推导准确率 91.8% → 93.4%（61 例已知答案）

Refs: #7
```

---

## 2. 分支策略

| 分支 | 用途 |
| --- | --- |
| `main` | 始终可运行、自检全绿。**禁止直接推送未通过自检的提交** |
| `feat/<主题>` · `fix/<主题>` | 特性与修复分支，合入 `main` 前须通过完整自检 |

提交粒度：**一次提交只做一件事**。数据重建与代码改动若互相独立，分开提交。

---

## 3. 提交前强制自检

```bash
npm test
```

等价于依次执行：

| 命令 | 校验内容 |
| --- | --- |
| `npm run verify` | 独立交叉校验 C01–C12（Python，`--strict`） |
| `npm run check:imports` | 模块图完整性（相对导入全部可解析） |
| `npm run test:logic` | 领域层与布局不变量（纯函数，无需 DOM） |
| `npm run test:render` | 端到端 DOM 渲染自检（需 jsdom，开发期依赖） |

四套必须**全部通过**方可提交。自检脚本是这套谱系数据的唯一回归防线——
原谱只有一份，任何"看起来对"的改动都必须由断言背书。

---

## 4. 数据类变更的额外要求

数据是本项目的核心资产，其变更比代码变更更需谨慎。

1. **原始转录永不修改**。`data/source/` 是基线，任何调整只能发生在建模层
   （`tools/build_data.py`），并只影响下游 `data/genealogy.json`。
2. **不猜原则**。无法判定处一律留白并标记待考，不臆造结论。推导出的关系必须
   标记 `parentEvidence: 'heuristic'` 并在校验中心集中列出。
3. **禁止按姓名合并人物**。凡字命名下同辈共用一字，「同名 + 同世代」≠ 同一人。
   身份判定的唯一依据是递归身份键 `pkey = (世代, 姓名, pkey(父))`。
4. **变更后须重建并同步数字**。跑 `npm run build:data`，然后核对并同步
   `README.md` 与 `docs/` 中受影响的人数、关系数、校验项数等统计值。
5. **新增校验规则须给出编号**。构建期续编 `V0xx`，运行时续编 `F-`/`R-`/`G-`，
   并在 `docs/03-数据模型与校验规范.md` 中登记。

---

## 5. 不应入库的内容

见 `.gitignore`。要点：

- `node_modules/`（开发期依赖，非站点依赖）；
- `data/*.bak*.json`（建模调试快照，可由 `data/source/` 重放生成）；
- `~$*`（Word/WPS 打开文档时产生的锁文件）；
- `.workbuddy-ai/`（本地智能体工作记忆与会话状态，含本机绝对路径）。
